# Todos

Todos of branch `telemetry`.

1. pure rust codegen
2. codes for telemetry/v1alpha1
3. codes for operator/v1alpha1
4. strict exported CRDs following `kubernetes/customresourcedefinitions.gen.yaml` in `istio/api` generated by cue-gen

parse OpaiInfo from `openapi-json` dir or `output` dir
1. go to dir: OUTPUT_DIR / ISTIO_VERSION / API_GROUP / API_VERSION / RESOURCE
2. remove all but `src/models` & `src/lib.rs`